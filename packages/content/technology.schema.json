{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TechnologyDef",
  "type": "object",
  "properties": {
    "display_name": {
      "description": "Player-facing name shown in the research UI.",
      "type": "string"
    },
    "effects": {
      "description": "Ordered effects used when calculating the owning player's sensor coverage while this technology is owned,\nincluding technologies granted at spawn. Omitted means no stat changes (the technology can still unlock prerequisites).",
      "type": "array",
      "items": {
        "$ref": "#/$defs/TechnologyEffect"
      }
    },
    "granted_on_spawn": {
      "description": "Grant this technology to every player at spawn; it cannot have a research cost.",
      "type": "boolean",
      "default": false
    },
    "requires": {
      "description": "Prerequisite technology or an all/any group of prerequisites.",
      "anyOf": [
        {
          "$ref": "#/$defs/TechnologyRequirement"
        },
        {
          "type": "null"
        }
      ]
    },
    "research_cost": {
      "description": "Total research cost, keyed by resource ID. Spent progressively at research_rates until research completes.\nSpawn-granted technologies must omit this; researchable technologies need a non-empty cost.",
      "type": "object",
      "additionalProperties": {
        "type": "number",
        "format": "float"
      }
    },
    "research_rates": {
      "description": "Research spending in resource units per second, keyed by resource ID. Missing resources default to 1/s.\nCompletion time is the largest cost/rate; insufficient resources pause progress.",
      "type": "object",
      "additionalProperties": {
        "type": "number",
        "format": "float"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "display_name"
  ],
  "$defs": {
    "TechnologyEffect": {
      "type": "object",
      "properties": {
        "operation": {
          "description": "How the value changes the target: add, multiply, set, or cap.",
          "$ref": "#/$defs/TechnologyEffectOperation"
        },
        "target": {
          "description": "Property modified for the owning player. Only `entity.sensor.range` is supported.\nApplied to every owned entity's base sensor range (0 if absent), so an additive effect can create sensor coverage.",
          "type": "string"
        },
        "value": {
          "description": "Finite operand: world units for add/set/cap of sensor range, or a dimensionless factor for multiply.",
          "type": "number",
          "format": "float"
        }
      },
      "additionalProperties": false,
      "required": [
        "target",
        "operation",
        "value"
      ]
    },
    "TechnologyEffectOperation": {
      "oneOf": [
        {
          "description": "Adds value to the current target value.",
          "type": "string",
          "const": "add"
        },
        {
          "description": "Multiplies the current target value by value.",
          "type": "string",
          "const": "multiply"
        },
        {
          "description": "Replaces the current target value with value.",
          "type": "string",
          "const": "set"
        },
        {
          "description": "Sets the target to the smaller of its current value and value (an upper limit).",
          "type": "string",
          "const": "cap"
        }
      ]
    },
    "TechnologyRequirement": {
      "anyOf": [
        {
          "description": "ID of a technology the player must own.",
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "all": {
              "description": "All listed prerequisites must be satisfied. Use a non-empty list of technology IDs or nested all/any groups.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/TechnologyRequirement"
              }
            }
          },
          "required": [
            "all"
          ]
        },
        {
          "type": "object",
          "properties": {
            "any": {
              "description": "At least one listed prerequisite must be satisfied. Use a non-empty list of technology IDs or nested all/any groups.",
              "type": "array",
              "items": {
                "$ref": "#/$defs/TechnologyRequirement"
              }
            }
          },
          "required": [
            "any"
          ]
        }
      ]
    }
  }
}